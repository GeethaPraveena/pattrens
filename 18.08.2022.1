
#include <stdio.h>
int main()
{
   int a[100],i,n,flag=0;
   scanf("%d",&n);
   for(i=0;i<n;i++)
    scanf("%d",&a[i]);
     for(i=0;i<n;i++)
     {
         if()
         {
         flag=1;
         }
         
     }
     if(flag==0)
     printf("unique");
     else
     printf("dupilicate");

    return 0;
}
