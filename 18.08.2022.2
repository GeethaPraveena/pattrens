#include <stdio.h>
int main()
{
    int a[100],i,n,f=1,sum=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
    for(i=0;i<n;i++)
    {
        f=f*a[i];
        sum=sum+f;
        
    }
   printf("%d",sum);
    return 0;
}
